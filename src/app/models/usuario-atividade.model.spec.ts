import { UsuarioAtividade } from './usuario-atividade.model';

describe('UsuarioAtividade', () => {
  it('should create an instance', () => {
    expect(new UsuarioAtividade()).toBeTruthy();
  });
});
